#include <stdio.h>

int heikin(int n);

int main(void)
{
      int n;
      scanf("%3d", &n);
      heikin(n);
      printf("%d\n", n);
      return 0;
}

int heikin(int n)
{
      n=1110-n;
      return 0;
}
