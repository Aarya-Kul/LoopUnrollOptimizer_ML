#include <stdio.h>

int henkan(int);

int main()
{
      int n;
      scanf("%d", &n);
      heikin(n);
      printf("%d\n", n);
      return 0;
}

int heikin(int n)
{
      n=1110-n;
      return;
}
